/yolo_core_node:
  ros__parameters:
    conf_threshold: 0.45
    iou_threshold: 0.5

    detector_type: "bbox"      # 지원 옵션: bbox, obb, seg
    dispatcher_type: "custom"    # 지원 옵션: turtlebot, custom
    
    depth_filter_size: 3       # 뎁스 중간값 필터 커널 크기(3x3, 5x5 등 홀수)